<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to 'Supplejack', root_path, class: "navbar-brand" %>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbar-nav"
      aria-controls="navbar-nav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <% content_partners_classes = class_names('nav-link', active: request.path.include?('content_partners')) %>
    <% destinations_classes = class_names('nav-link', active: request.path.include?('destinations')) %>
    <% extraction_jobs_classes = class_names('nav-link', active: current_page?(extraction_jobs_path)) %>

    <div class="collapse navbar-collapse" id="navbar-nav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Content Partners', content_partners_path, class: content_partners_classes %>
        </li>
        <li class="nav-item">
          <%= link_to 'Destinations', destinations_path, class: destinations_classes %>
        </li>
        <li class="nav-item">
          <%= link_to 'Jobs', extraction_jobs_path, class: extraction_jobs_classes %>
        </li>
      </ul>
    </div>
  </div>
</nav>
