<%= content_for(:title) { 'New Schedule' } %>

<%= content_for :header do %>
  <%= render 'shared/breadcrumb_nav' %>

  <h1 class="header__title">New schedule</h1>

  <div class="float-end">
    <button type="submit" class="btn btn-primary" data-form="js-schedule-form">
      Save schedule
    </button>

    <%= link_to 'Cancel', pipeline_schedules_path(@pipeline), class: 'btn btn-outline-danger' %>
  </div>

  <div class="clearfix"></div>

<% end %>

<div class='mb-4'></div>

<%= render 'form' %>
