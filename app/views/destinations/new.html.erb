<%= content_for(:title) { 'New Destination' } %>

<%= content_for :header do %>
  <%= render 'shared/breadcrumb_nav' %>

  <h1 class="header__title">Add New Destination</h1>
<% end %>

<%= render 'form' %>
